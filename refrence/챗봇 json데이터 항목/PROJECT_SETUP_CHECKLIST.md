# 바로빌 챗봇 프로젝트 셋업 체크리스트

> **이 문서는**: Cursor AI로 프로젝트를 이전하고 개발을 계속하기 위한 완전한 체크리스트입니다.

---

## ✅ Phase 1: 파일 구조 설정

### 1-1. 프로젝트 루트 디렉토리 생성
```bash
mkdir -p barobill-chatbot
cd barobill-chatbot
```

### 1-2. 기본 디렉토리 구조 생성
```bash
mkdir -p src/data
mkdir -p src/components
mkdir -p src/utils
mkdir -p tools
mkdir -p docs
mkdir -p public
```

### 1-3. 파일 배치

| 파일 | 복사할 위치 | 설명 |
|------|-------------|------|
| `barobill-knowledge.json` | `src/data/` | 지식베이스 JSON |
| `BAROBILL_CHATBOT_DATA_TEMPLATE.xlsx` | `tools/` | 엑셀 템플릿 |
| `excel_to_json_converter.py` | `tools/` | 변환 스크립트 |
| `validate_knowledge_base.py` | `tools/` | 검증 스크립트 |
| `requirements.txt` | `tools/` | Python 의존성 |
| `BAROBILL_CHATBOT_DATA_STRATEGY.md` | `docs/` | 전략 문서 |
| `KNOWLEDGE_BASE_GUIDE.md` | `docs/` | 가이드 문서 |
| `README_CURSOR_AI.md` | `/` (루트) | 메인 README |
| `barobill-ai-chatbot-fixed.html` | `public/` | 최신 HTML |

**체크**: 
- [ ] 모든 파일이 올바른 위치에 있는가?
- [ ] 파일 권한이 올바른가? (실행 파일: chmod +x)

---

## ✅ Phase 2: 개발 환경 구축

### 2-1. Node.js 프로젝트 초기화 (이미 있다면 스킵)
```bash
# package.json이 없다면
npm init -y
```

### 2-2. Vite 및 의존성 설치
```bash
# Vite가 없다면
npm install -D vite

# 기존 의존성 복구
npm ci
```

### 2-3. Python 환경 설정
```bash
# Python 버전 확인 (3.8 이상)
python --version

# 가상환경 생성 (선택)
python -m venv venv
source venv/bin/activate  # Mac/Linux
# 또는
venv\Scripts\activate  # Windows

# 의존성 설치
pip install -r tools/requirements.txt
```

**체크**:
- [ ] `npm run dev` 명령이 작동하는가?
- [ ] Python 스크립트가 실행되는가?
- [ ] `python tools/excel_to_json_converter.py --help` 작동하는가?

---

## ✅ Phase 3: 데이터 검증 및 통합

### 3-1. 기존 JSON 파일 검증
```bash
python tools/validate_knowledge_base.py -f src/data/barobill-knowledge.json
```

**체크**:
- [ ] 검증이 성공적으로 완료되었는가?
- [ ] 경고나 에러가 있다면 수정했는가?

### 3-2. 엑셀 템플릿 테스트
```bash
# 샘플 데이터로 변환 테스트
python tools/excel_to_json_converter.py \
  -i tools/BAROBILL_CHATBOT_DATA_TEMPLATE.xlsx \
  -o tools/test-output.json \
  --validate
```

**체크**:
- [ ] 변환이 성공적으로 완료되었는가?
- [ ] test-output.json 파일이 생성되었는가?
- [ ] 검증을 통과했는가?

---

## ✅ Phase 4: 챗봇 기능 테스트

### 4-1. 개발 서버 실행
```bash
npm run dev
```

### 4-2. 브라우저 테스트

**URL**: `http://localhost:8080` (또는 Vite가 지정한 포트)

**테스트 체크리스트**:
- [ ] 챗봇이 정상적으로 로드되는가?
- [ ] 인사말이 표시되는가?
- [ ] 빠른 질문 버튼이 작동하는가?
- [ ] 질문 입력 시 답변이 나오는가?
- [ ] 어투 변경이 작동하는가?
- [ ] FAQ 탭이 정상 작동하는가?
- [ ] 설정 탭이 정상 작동하는가?
- [ ] PDF 내보내기가 작동하는가?

### 4-3. 키워드 매칭 테스트

**테스트 질문**:
```
✅ "세금계산서 발급 방법" → Knowledge 답변
✅ "반품 처리는 어떻게 하나요?" → Case 답변
✅ "안녕하세요" → Intent 답변
✅ "부가세 신고 기한" → Knowledge 답변
```

**체크**:
- [ ] 모든 테스트 질문에 적절한 답변이 나오는가?
- [ ] 점수가 15점 이상으로 계산되는가? (콘솔 확인)

---

## ✅ Phase 5: Admin 페이지 테스트 (있다면)

### 5-1. Admin 접근
```bash
# 브라우저에서 접속
http://localhost:8080/admin
```

**체크**:
- [ ] Admin 페이지가 로드되는가?
- [ ] 항목 목록이 표시되는가?
- [ ] 새 항목 추가가 작동하는가?
- [ ] 항목 수정이 작동하는가?
- [ ] 항목 삭제가 작동하는가?
- [ ] JSON 다운로드가 작동하는가?

---

## ✅ Phase 6: 데이터 확장 실습

### 6-1. 새 질문 추가 (엑셀 방식)

```bash
# 1. 엑셀 템플릿 열기
open tools/BAROBILL_CHATBOT_DATA_TEMPLATE.xlsx

# 2. 새 행 추가
# ID: KB-TEST-001
# 구분: 개념
# 대분류: 테스트
# 질문: 이것은 테스트 질문입니다
# 우선순위: 5
# 키워드: 테스트,질문,확인
# (답변 3가지 작성)

# 3. 저장 후 변환
python tools/excel_to_json_converter.py \
  -i tools/BAROBILL_CHATBOT_DATA_TEMPLATE.xlsx \
  -o src/data/barobill-knowledge.json \
  --validate

# 4. 개발 서버 재시작
npm run dev

# 5. 브라우저에서 "테스트 질문" 입력하여 확인
```

**체크**:
- [ ] 새 질문이 정상적으로 추가되었는가?
- [ ] 챗봇이 새 질문을 인식하는가?

---

## ✅ Phase 7: Git 설정 및 버전 관리

### 7-1. Git 초기화 (이미 있다면 스킵)
```bash
git init
```

### 7-2. .gitignore 설정
```bash
cat > .gitignore << 'EOF'
# Node
node_modules/
dist/
.vite/

# Python
venv/
__pycache__/
*.pyc
.pytest_cache/

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# 임시 파일
tools/test-output.json
*.tmp
*.bak

# 환경 변수
.env
.env.local
EOF
```

### 7-3. 초기 커밋
```bash
git add .
git commit -m "chore: 바로빌 챗봇 프로젝트 초기 설정"
```

**체크**:
- [ ] .gitignore가 올바르게 설정되었는가?
- [ ] 불필요한 파일이 제외되었는가?

---

## ✅ Phase 8: 문서화 확인

### 8-1. 문서 목록 확인
```bash
ls -la docs/
ls -la README*.md
```

**필수 문서**:
- [ ] `README_CURSOR_AI.md` (메인 가이드)
- [ ] `docs/KNOWLEDGE_BASE_GUIDE.md` (상세 가이드)
- [ ] `docs/BAROBILL_CHATBOT_DATA_STRATEGY.md` (전략 문서)

### 8-2. 문서 링크 확인

각 문서의 내부 링크가 작동하는지 확인:
```bash
# Markdown 미리보기로 확인
code README_CURSOR_AI.md
```

**체크**:
- [ ] 모든 내부 링크가 작동하는가?
- [ ] 예시 코드가 올바른가?
- [ ] 연락처 정보가 정확한가?

---

## ✅ Phase 9: 팀 공유 및 온보딩

### 9-1. 기획팀 온보딩

**준비물**:
1. `BAROBILL_CHATBOT_DATA_TEMPLATE.xlsx` 템플릿
2. `docs/BAROBILL_CHATBOT_DATA_STRATEGY.md` 문서
3. 변환 스크립트 사용법 안내

**교육 체크리스트**:
- [ ] 엑셀 템플릿 작성법 설명
- [ ] ID 규칙 이해
- [ ] 우선순위 설정 원리
- [ ] 키워드 작성 팁
- [ ] 어투별 답변 작성법

### 9-2. 개발팀 공유

**공유 사항**:
1. Cursor AI 프로젝트 링크
2. `README_CURSOR_AI.md` 문서
3. Git 저장소 주소
4. 개발 서버 포트 정보

**체크**:
- [ ] 모든 팀원이 로컬 환경 구축 완료
- [ ] Git 권한 설정 완료
- [ ] Slack 채널 초대 완료

---

## ✅ Phase 10: 프로덕션 배포 준비

### 10-1. 빌드 테스트
```bash
npm run build
```

**체크**:
- [ ] 빌드가 성공적으로 완료되었는가?
- [ ] dist/ 폴더가 생성되었는가?
- [ ] 정적 파일이 올바르게 생성되었는가?

### 10-2. 프로덕션 환경 테스트
```bash
npm run preview
```

**체크**:
- [ ] 프로덕션 빌드가 정상 작동하는가?
- [ ] 모든 기능이 개발 환경과 동일하게 작동하는가?

---

## 📋 최종 체크리스트

### 파일 및 디렉토리
- [ ] 모든 소스 파일이 올바른 위치에 있음
- [ ] Python 스크립트가 실행 가능함
- [ ] 문서가 최신 상태로 업데이트됨

### 기능
- [ ] 챗봇이 정상 작동함
- [ ] 키워드 매칭이 올바르게 작동함
- [ ] 어투 변경이 작동함
- [ ] Admin 페이지가 작동함 (있다면)
- [ ] PDF 내보내기가 작동함

### 데이터 관리
- [ ] 엑셀→JSON 변환이 작동함
- [ ] JSON 검증이 작동함
- [ ] 지식베이스가 유효함

### 개발 환경
- [ ] npm run dev 작동함
- [ ] Hot Reload 작동함
- [ ] 빌드가 성공함

### 문서화
- [ ] README가 완전함
- [ ] 가이드 문서가 최신 상태
- [ ] 예시가 올바름

### 팀 협업
- [ ] Git 저장소 설정 완료
- [ ] 팀원 온보딩 완료
- [ ] Slack 채널 활성화

---

## 🎉 완료!

모든 항목을 체크했다면 프로젝트가 Cursor AI에서 개발할 준비가 완료되었습니다!

### 다음 단계

1. **데이터 확장**: 기획팀과 협력하여 지식베이스 확장
2. **기능 고도화**: Fuzzy Matching, Context Awareness 등 추가
3. **피드백 수집**: 사용자 피드백 분석 및 반영
4. **배포**: 프로덕션 환경 배포

---

**문서 버전**: v1.0  
**최종 수정일**: 2025-01-15  
**작성자**: AI Development Team
